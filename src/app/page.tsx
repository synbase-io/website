import { Suspense } from "@/component/common/suspense";

const Page = async () => {
    return (
        <div className="h-64">
            <Suspense>Muh</Suspense>
        </div>
    );
};

export default Page;
